const inquirer = require("inquirer");
const fs = require("fs");
const path = require("path");

// create function for Manager - find questions for each team member i.e. - name, id, email, office ste #

// create function for Engineer - name, id, email, GitHub username

// create function for Intern - name, id, email, name of school

// create function for building out the team - create var array of all team members

// create path to build in HTML for static webpage
